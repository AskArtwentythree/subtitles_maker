"""Умный авто-монтаж IRL-контента.

Пайплайн (движко-независимый «мозг»):
  1. probe   — параметры видео (размер, fps, длительность).
  2. beats   — единицы монтажа = сегменты транскрипта + оценка качества кадров.
  3. vision  — что в кадре, насколько годен, где фокус, что подписать.
  4. brain   — LLM собирает EDL (Edit Decision List): что оставить, переходы,
               зумы, подписи, акценты — с правилом «не перебарщивать».
  5. build_edl — оркестратор: video.mov -> edl.json.

EDL потом потребляют рендереры (Remotion / HyperFrames), создавая один и тот же
ролик в двух движках.
"""
